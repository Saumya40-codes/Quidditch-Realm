<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <script>
        var status = '<%- JSON.stringify(status) %>';
        if (JSON.parse(status) == "verified") {
            if (confirm("Redirect to the login page?")) {
                window.location.href = "https://quidditch-realm-five.vercel.app/login";
            }
        }
    </script>
    <style>
        html, body {
            height: 100%;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #422b72 0%, #734b6d 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: Arial, sans-serif;
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 500px;
            width: 580px;
            border-radius: 20px;
            background: transparent;
            box-shadow: 6px 6px 6px 5px rgba(0, 0, 0, 0.5);
        }

        h1 {
            text-align: center;
            color: #fff;
            font-family: italic;
            color:black;
            padding: 10px;
            border-radius: 20px 20px 0 0;
            margin: 0;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            width: 100%;
            background: transparent;
        }

        form label {
            margin-top: 20px;
            color: #333;
            font-weight: bold;
            margin-bottom: 10px;
        }

        input[type="password"] {
            padding: 8px;
            width: 300px;
            border-radius: 4px;
            border: 1px solid #ccc;
        }

        input[type="submit"] {
            margin-top: 30px;
            padding: 15px 25px;
            border: none;
            border-radius: 6px;
            background-color: #007bff;
            color: #fff;
            font-weight: bold;
            cursor: pointer;
        }

        input[type="submit"]:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Reset Password</h1>
        <form method="post">
            <label for="password">Password</label>
            <input type="password" name="password" id="password">
            <label for="confirmPassword">Confirm Password</label>
            <input type="password" name="confirmPassword" id="confirmPassword">
            <input type="submit" value="Submit" id="submit">
        </form>
    </div>
</body>
</html>
